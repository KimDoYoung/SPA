node + typescript
=================

# 설치

1. npm i -g typescript
tsc --version

2. tsc --init
    "module": "commonjs",
    "rootDir": "./src", 
    "moduleResolution": "node",
    "outDir": "./dist",    

3. npm init -y
4. npm instal express